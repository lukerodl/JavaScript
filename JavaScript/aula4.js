/*var numberString = prompt("Insira um número abaixo")
var realnumber = parseInt(numberString)

if(realnumber % 2 === 0)
alert("O número " + realnumber + " é par")
else {
    alert("o número " + realnumber + " é impar")
}

function number() {
    alert(realnumber)
}

number ()
number ()
number ()

function name(nome) {
    console.log(nome)
}

name("Lucas Leite")
name("Luan Leite")


//Declaração de variável para armazenar o resultado da soma
var resultado = 0;

//Função que soma dois números e atribui o resultado
function somar(primeiroNumero, segundoNumero) {
    resultado = primeiroNumero + segundoNumero
}

//Função que mostra o resultado no console
function mostrar(mensagem) {
    console.log(mensagem)
}

//Chamamos primeiro para somar e depois para mostrar
somar(6, 3);
    mostrar(resultado);

    function somar(num1, num2) {
        return num1 + num2;
    }
    var resultado = somar(5,8)
    console.log(resultado)


    function calculadora(primeiroNumero, segundoNumero, operacao) {
        if (operacao == "+") {
            return primeiroNumero + segundoNumero;
        }
        else if (operacao == "-") {
            return primeiroNumero - segundoNumero;
        }
        else if (operacao == "*") {
            return primeiroNumero * segundoNumero;
        }
        else if (operacao == "/") {
            return primeiroNumero / segundoNumero;
        }
        else {
            return 0;
        }
    }

    var n1  = prompt("insira o primeiro número")
    var n2  = prompt("insira o segundo número")
    var n3  = prompt("insira a operação")
    alert(calculadora(n1, n2, n3))


    function subtrair(num1, num2) {
        let resultado;
        if(num1 > num2) {
            resultado = num1 - num2;
            console.log("o primeiro digito é maior que o segundo")
        }
        else {
            resultado = num2 - num1;
            console.log("o segundo é maior que o primeiro")
        }
    }

    let numero1 = prompt("digite um número")
    let numero2 = prompt("digite um número novamente")
    subtrair(numero1, numero2);*/ 